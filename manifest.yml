---
applications:
  .:
    name: rodovias
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    infra: aws
    url: ${name}.${target-base}
    mem: 128M
    instances: 1
    services:
      mysql-4e326:
        type: mysql
